<!--
  This view is used to render the plugin list page.

  Since we don't really have anything dynamic here, let's just use static HTML. 
-->
<div>
  This plugin extends ProcessKiller extension point and sends signal (currently SIGKILL) to given job via native GNU C library call. On Windows, it's forking a command line and calling the taskkill command. 
</div>